# Optional canonical origin for non-Vercel production deployments.
# Leave empty in local development so metadata uses http://localhost:3000.
SITE_URL=

# Local-only RustFS administrator credentials used by compose.yaml.
# Set both to unique values before starting the service.
RUSTFS_ACCESS_KEY=
RUSTFS_SECRET_KEY=
