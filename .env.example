# Absolute production origin used for canonical URLs and metadata.
SITE_URL=https://example.com

# Local-only RustFS administrator credentials used by compose.yaml.
# Set both to unique values before starting the service.
RUSTFS_ACCESS_KEY=
RUSTFS_SECRET_KEY=
